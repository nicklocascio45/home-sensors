set(srcs)
set(include_dirs "include")

list(APPEND srcs "display.c")

idf_component_register(SRCS "${srcs}"
                       INCLUDE_DIRS "${include_dirs}"
                       PRIV_REQUIRES esp_timer esp_lcd lvgl)
